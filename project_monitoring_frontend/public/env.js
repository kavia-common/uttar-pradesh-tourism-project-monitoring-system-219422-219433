window.__env = window.__env || {
  // This value can be modified in previews or production deployments without rebuilding.
  // Example: 'https://api-preview.example.com'
  apiBaseUrl: 'http://localhost:3001'
};
